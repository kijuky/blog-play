{
  "name": "Shell (Minimal)",
  "scopeName": "source.shell",
  "patterns": [
    { "include": "#comments" },
    { "include": "#strings" },
    { "include": "#variables" },
    { "include": "#keywords" },
    { "include": "#numbers" }
  ],
  "repository": {
    "comments": {
      "patterns": [
        { "name": "comment.line.number-sign.shell", "match": "#.*$" }
      ]
    },
    "strings": {
      "patterns": [
        { "name": "string.quoted.double.shell", "begin": "\"", "end": "\"", "patterns": [ { "match": "\\\\.", "name": "constant.character.escape.shell" } ] },
        { "name": "string.quoted.single.shell", "begin": "'", "end": "'" }
      ]
    },
    "variables": {
      "patterns": [
        { "name": "variable.other.shell", "match": "\\$\\{?[A-Za-z_][A-Za-z0-9_]*\\}?" }
      ]
    },
    "keywords": {
      "patterns": [
        { "name": "keyword.control.shell", "match": "\\b(if|then|else|elif|fi|for|while|do|done|case|esac|in|function|select|until|time)\\b" },
        { "name": "support.function.shell", "match": "\\b(echo|printf|cd|pwd|exit|export|unset|alias|source|test|true|false)\\b" }
      ]
    },
    "numbers": {
      "patterns": [
        { "name": "constant.numeric.shell", "match": "\\b\\d+(?:\\.\\d+)?\\b" }
      ]
    }
  }
}
